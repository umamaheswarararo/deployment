---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-api
  labels:
    app: client-api
spec:
  selector:
    matchLabels:
      app: client-api
  replicas: ${CLIENT_API_REPLICAS}
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: client-api
    spec:
      containers:
        - name: client-api
          image: registry.digitalocean.com/tectoro/client-reports-api:${CLIENT_API_VERSION}
          envFrom:
            - configMapRef:
                name: clientapi
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: 50Mi
              cpu: "20m"
            requests:
              memory: 10Mi
              cpu: "10m"
      imagePullSecrets:
        - name: doregistry
---
apiVersion: v1
kind: Service
metadata:
  name: client-api
spec:
  selector:
    app: client-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
---
